import type { WorkflowType } from './types';

export const WORKFLOW_LABELS: Record<WorkflowType, string> = {
  rename_upload_drive: 'Rename & upload to Google Drive',
  append_sheet_row: 'Add row to Google Sheet'
};
